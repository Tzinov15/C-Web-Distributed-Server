CLIENT

  IMPLEMENT 

  ** fix issue with files that are too big for one buffer and need to be windowed
    GET  (assemblign file into one once received all chunks from the DFS server )
    LIST



SERVER


  PUT 
  ** look at the username name and password, validate request
  ** fix issue with files that are too big for one buffer and need to be windowed

  GET 
  ** look at the filename from the header
  ** need to assemble the file together

  LIST
  ** traverse directory of all four directorys, make vector array
